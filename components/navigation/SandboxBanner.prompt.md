Use `SandboxBanner` on every screen of a demo or sample-data environment.

```jsx
<SandboxBanner />
<AppChrome brandName="Spacely Sprockets Franchising" badge={<Badge tone="warning">SANDBOX</Badge>} />
```

Never make it dismissible, never shorten it to "Demo", and never reuse the band color anywhere else.
