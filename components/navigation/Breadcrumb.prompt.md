Use `Breadcrumb` on every browse view. It is the main reason the product still feels like a folder tree.

```jsx
<Breadcrumb items={[{ label: "Spacely Sprockets" }, { label: "Callahan Development Group" }, { label: "Unit 12" }]} />
```
