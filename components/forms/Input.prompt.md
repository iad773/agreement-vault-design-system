Use `Input` for search and all single-line entry.

```jsx
<Input icon={<SearchIcon />} placeholder="Search titles and document contents" />
<Input label="Effective date" hint="As written on the executed agreement" />
```

Placeholders describe what the field searches, in plain words. Focus shows the 2px steel ring; invalid turns the 1.5px border red and tints the hint.
